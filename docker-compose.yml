version: "3.8"

services:
  clinica-odont:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - ./:/app
      - /app/node_modules
    ports:
      - "3000:5173"
    command: >
      sh -c "npm install && npm run dev -- --host 0.0.0.0"
